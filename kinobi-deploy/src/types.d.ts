/// <reference types="bun-types" />

// Bun global types (commented out for Val Town compatibility)
// declare global {
//   const Bun: typeof import("bun");
// }

// Removed duplicate bun:sqlite module declaration to avoid duplicate identifier error.